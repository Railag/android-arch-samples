<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".mvvm.MainActivityMvvm"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="16dp">

    <Button
        android:id="@+id/mvpButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mvp_button"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/mvvmButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mvvm_button"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/mviButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mvi_button"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/cleanButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/clean_button"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

</LinearLayout>